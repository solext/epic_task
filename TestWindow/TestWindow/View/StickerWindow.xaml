<Window x:Name="loc" x:Class="TestWindow.View.StickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWindow.View"
        mc:Ignorable="d"
        Title="StickerWindow" Height="300" Width="300" >
    <Grid>
        <ComboBox x:Name="AppsComboBox" VerticalAlignment="Top" SelectedValue="{Binding SelectedProcessStr}" ItemsSource="{Binding RunningAps}" Margin="10,10,82,0" Height="20"/>
        <Button x:Name="RefreshBtn" Content="Refresh" Command="{Binding UpdateAppsCmd}" VerticalAlignment="Top" Margin="215,10,10,0"/>
    </Grid>
</Window>
